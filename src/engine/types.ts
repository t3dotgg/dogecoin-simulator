export const GAME_STORAGE_KEY = "game-storage";

export enum RealEstate {
  Server = "Server",
  MemeFactory = "Meme Factory",
  Pool = "Pool",
}

export enum Unlocks {
  SocialMediaManager = "Social Media Manager",
  Incorporate = "Incorporate",
}

export type CurrentMission = "To The Moon" | null;
